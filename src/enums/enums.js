import Enum from "es6-enum"

export const FORM_MODE = Enum("New", "Edit");
export const CONTENTS = Enum("HomeWelcome", "TargetForm", "Home", "Chat", "UserProfile");
export const ALERT_GOALS = Enum("SignInPage", "SideBarContainer");
